// Minimal TypeScript source file for the ci-app smoke test fixture.
// The inventory pass reads this directory; it needs at least one .ts file
// to detect the language as "typescript".

export const appName = 'ci-app';
